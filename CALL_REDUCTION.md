# DeRoute 调用量优化（2026-09-11）

本轮优先减少没有增加有效信息的模型往返。修改集中在规划调度、协议字段修复、重复核验、失败恢复、跨任务并发及断点恢复；小模型权重和量化配置不变。

## 为什么先改这些

此前 100 题日志按每题最后记录去重后，共 1776 次逻辑调用，其中规划 1122 次；规划中 368 次返回 `wait`，288 次动作被拒绝。问题并非主要出在小模型执行慢，而是规划、失败恢复和验证反复往返。完整统计口径见 [诊断报告](../DeRoute_vs_DecomP_诊断报告.md)。

旧版 `--resume` 每次授予新的规划、总调用和时间预算，失败题可不断消耗新额度。新版预算覆盖一题的整个生命周期。这会使过去靠多轮续跑才完成的题更早结束，因此必须同时检查调用数和准确率。

最新版 100 题总结记录的是后续 DeRoute v2：334 次 planner、226 次大模型作答、23 次大模型复核和 187 次小模型调用，EM（含别名）56%。24 次 DeepSeek 调用抽样的 prompt 缓存命中率为 95.3%，所以本轮**没有删减 30-shot**。固定指令和示例继续位于 prompt 前缀，可变工作流状态位于末尾；削减示例已从默认方案降为需单独验证准确率的消融项。

同一日志还显示高修订任务收益很低：66 题没有修订，其中 46 题答对；25 题累计修订至少 2 次，只完成 4 题、答对 2 题，并消耗 369 次模型调用。当前因此增加整题累计 3 次修订上限，阻止失败在多个节点之间反复转移。这个阈值来自单次 100 题观察，必须在新输出目录中复测 EM。

## 已实现的变化

| 环节 | 新行为 | 作用与边界 |
| --- | --- | --- |
| 规划等待 | 默认执行完当前前沿后才再调用 planner | 不再发请求询问是否需要 `wait`；等待由线程调度完成 |
| 批量规划 | `add_batch` 默认最多 3 个必要节点，整批验证成功后提交 | 可包含独立节点及指向同批较早节点的符号依赖；依赖答案仍由执行器实际求出 |
| 必要并行 | 批内就绪节点并行；确有额外独立工作时可显式 `continue_planning:true` | 只有执行池有容量才提前规划，避免默认轮询；本地 Qwen 仍最多一个并发 |
| 规划协议 | 从 `#k` 确定依赖，添加时补齐省略的顺序 id；提供合法动作、目标和错误历史 | 不为确定的元数据重新调用模型；未知引用、环、错误年份及不完整最终链仍被拒绝 |
| 执行协议 | 允许完整 JSON 外侧的代码围栏；从实际输入补齐 `used_inputs`，记录 `protocol_repairs` | 仅修复可确定的结构字段；原文引文、支持类型和假设仍校验。`used_inputs` 表示传入依赖，不证明语义上正确使用了它们 |
| 节点复核 | `reason` 的直接答案通过事实提取级别的引文/答案校验后，不再额外复核 | 间接推断、无法机械确认的 `reason` 以及小模型推断仍需要大模型复核；已有候选接管即算一次复核 |
| 最终核验 | 默认由本次 `finish.reason` 同时完成最后关系及答案类型检查 | 缺理由时回退独立核验；`final_check_mode="always"` 保留独立终检；图校验和所有者/所在地规则始终执行 |
| 无信息重跑 | 大模型已失败的同一查询，仅改变 `reason` 不允许重跑 | 必须改变问题、操作、依赖、证据或输出要求；不是对所有语义同义改写的完整识别 |
| 暂停恢复 | 保留失败候选和接管/复核阶段；多次暂停也不会丢失早先候选 | 从大模型阶段继续，无需先重跑已经失败的小模型或重新生成待复核候选 |
| 跨任务并发 | 默认同时推进 4 题，共享一个 Qwen 实例及 DeepSeek 并发限流器 | 把一题的本地推理时间与其他题的 API 等待重叠；不改变题内依赖，也不会增加单题调用预算 |
| 并发检查点 | 每题写入独立原子快照，主线程串行追加工作流日志 | 避免并发任务覆盖同一个 `checkpoint.json`；续跑逐题恢复且不重复已成功任务 |
| 小模型模式 | 默认 `cost` 保留所有合格 lookup；可选 `parallel_only` 只在题内并行前沿启用一个 Qwen 节点 | `cost` 优先少用大模型 API；`parallel_only` 优先缩短串行延迟，但会把串行 lookup 转成大模型调用 |
| 修订上限 | 每节点最多 2 次，同时一题累计最多 3 次 | 总额耗尽后不再允许 `revise`，减少低收益的规划、重跑和复核 |
| 预算与终止 | 规划次数、总调用和运行时间跨续跑累计；预算耗尽、abort、修订耗尽等终止题在 `--resume` 时跳过 | 网络/额度等可恢复异常可继续；已终止样本不会因重复运行命令而得到新预算，也不重复追加同一结果 |
| 统计 | 保留缓存命中/未命中 token；记录整批实际墙钟；`compare_runs.py` 同时展示调用、缓存和两种耗时 | 并发后各题耗时之和包含重叠，性能对比应使用 `run_metrics.json` 的批次墙钟 |
| 预声明终点 | `finish_after_success` 可与 `add`/`add_batch` 同时提交 | 仅对未修订、未接管/复核、全链直接证据的完整图自动结束，否则恢复增量 planner |
| 自适应小模型证据 | 保留 top-3，字符预算允许时最多扩展到 top-5 | 不增加 Qwen 调用，尝试减少检索遗漏导致的 DeepSeek 接管 |
| 请求重试 | 408/429/5xx/网络错误最多退避重试 2 次 | 可避免整题暂停后续跑再规划；同时单独统计逻辑调用与实际 HTTP 请求 |
| 按用途输出上限 | planner 384，answer/review 512，final check 384 | 不用全局 256 截断正常结果；该项防异常长输出，不直接减少调用数 |
| 耗时拆分 | 大模型记录限流排队/服务/退避，Qwen 记录 GPU 锁排队/推理 | 后续根据实测决定 DeepSeek 并发和 5090 上的 Qwen 服务方式，不凭累计时间推测 |

关键默认参数位于 `model.json`：

```json
{
  "runtime": {
    "max_batch_nodes": 3,
    "max_total_revisions": 3,
    "max_inflight_tasks": 4,
    "final_check_mode": "planner",
    "auto_finish_mode": "safe"
  },
  "routing": {
    "small_route_mode": "cost",
    "small_top_k": 3,
    "small_max_top_k": 5
  }
}
```

`max_batch_nodes` 支持 1–8，`max_inflight_tasks` 支持 1–16；`final_check_mode` 支持 `planner`、`always`；`auto_finish_mode` 支持 `safe`、`off`；`small_route_mode` 支持 `cost`、`parallel_only`。旧自定义配置未包含新增字段时会补入默认值。24 次规划、48 次逻辑模型调用及 600 秒为每题累计上限；请求层重试单独计入物理请求数。在途请求仍会等待完成或超时，因此运行时间可能略超上限。

## 离线验证

在已有 conda `rag310` 环境运行：

```bash
bash agent.sh -m unittest discover -s tests -v
```

**100 项测试通过**，包括真实客户端接口调用次数、安全自动结束、非法终点声明的批次原子性、退避重试与物理请求统计、按用途输出上限、自适应检索、大小模型并行、跨任务并发、逐任务续跑、跨恢复预算、缓存 token 保存及独立终检开关。测试客户端是模拟模型，不发远程请求，也不加载 Qwen 权重。

新增的确定性两跳用例在两个节点均通过直接证据校验时，使用 `finish_after_success` 将 planner 从 2 次降为 1 次，总调用从 4 次降为 3 次。这只证明调度器确实省掉了末尾调用，不能外推为 100 题节省比例。

同一个确定性模拟任务“两个独立分支，再汇合”使用修改前备份与当前实现分别执行：

| 指标 | 修改前 | 修改后 |
| --- | ---: | ---: |
| 全部调用 | 11 | 6 |
| 规划调用（包含 wait） | 6 | 3 |
| 其中 wait | 3 | 0 |
| 节点执行 | 3 | 3 |
| 额外节点复核 | 1 | 0 |
| 独立最终核验 | 1 | 0 |
| 流程结果 | 成功 | 成功 |
| 输出 | AlphaTown and BetaTown | AlphaTown and BetaTown |

本例调用减少 **45.5%**。这是可复现的调度与协议测试结果，不能外推为真实 100 题的节省比例或 EM。结果文件：[call_reduction_offline_comparison.json](outputs/call_reduction_offline_comparison.json)。对照脚本位于 `../.agents/deroute_call_reduction/offline_comparison.py`，修改前备份路径记录在同目录的 `progress.json`。

独立最终核验模式在同一新版测试中是 7 次调用。将核验合并进规划会减少一次独立语义检查；代码校验引文与答案词并不能完全证明关系蕴含。真实评测应同时记录 EM/F1、完成率、失败类型及调用量，不能仅以调用数下降判断整体改进。

## 新实验与恢复

**协议已升至版本 12，必须使用新的输出目录，不能直接续跑旧版检查点。** 本轮没有启动新的付费模型评测，也没有修改历史实验结果。

先在此前相同 100 题文件中试跑前 5 题：

```bash
cd /home/suibo/HiNA/DeRoute
bash agent.sh run.py run --input outputs/parallel100_subset.jsonl --limit 5 \
  --output outputs/devtest_random100_flash_v4_smoke --live --task-workers 4
```

需要跑全部 100 题时，用同一配置和输出目录：

```bash
bash agent.sh run.py run --input outputs/parallel100_subset.jsonl --all \
  --output outputs/devtest_random100_flash_v4 --live --task-workers 4
```

中断后在相同命令追加 `--resume`。每题状态位于 `checkpoints/`，本次及跨续跑累计墙钟位于 `run_metrics.json`。`--resume` 只继续尚可恢复的任务，跳过已成功和已终止的失败任务。若希望以新预算重新做实验，请明确使用新输出目录。更改终检模式、模型配置或提示词也需要新目录，避免混入不同实验条件。

若要验证最新版总结提出的“串行不用小模型”，必须使用另一个输出目录：

```bash
bash agent.sh run.py run --input outputs/parallel100_subset.jsonl --all \
  --output outputs/devtest_random100_flash_v4_parallel_only --live --task-workers 4 \
  --small-route-mode parallel_only
```

这个模式预计减少本地小模型调用和延迟，但会增加大模型 API 调用；当前主要目标是减少大模型调用，因此默认仍是 `cost`。两组实验都应比较 EM、完成率、大模型调用、缓存未命中 token 和 `batch_wall_seconds`。

小模型推理引擎、精度和显存行为本轮未调整；减少无意义的大模型调用不等于已经解决小模型的加载与生成耗时。后续可在调用流程稳定后单独测量这些因素。
