# DeRoute 修改日志

记录**已完成的改动**与**待做建议**。建议按优先级排列,每条给出实测证据、动作与风险。

数据出处:`../DeRoute_vs_DecomP_devtest100_实验总结.md`(该文件位于仓库之外,不在版本控制内)。
统一口径:100 题 `eval_set/devtest_random100_seed42.jsonl`,DeepSeek-V4-Pro @ 并行科技端点,小模型 Qwen2.5-7B-Instruct @ 本地 vLLM。

> ⚠️ **单次运行 EM 噪声约 ±8 pt**(DecomP 同配置 6 次运行跑出 33%~39%)。
> 因此任何单次运行的 EM 差异都不可解读,只能看墙钟、调用数与填充率。

---

## 待做建议

### P0-1 把任务层并发固化进 `model.json`

| 项 | 内容 |
|---|---|
| 现状 | `--task-workers 8` **只是命令行参数**,`model.json` 的 `runtime.max_inflight_tasks` 仍是 4 |
| 证据 | tw8 480.4s vs tw4 554.0s(同日、同端点、同配置),**−13.3%**;填充率 3.44 → 3.88 |
| 动作 | `"runtime": { "max_inflight_tasks": 8 }`,**`large.max_concurrent` 保持 4 不变** |
| 风险 | 低。代价是缓存命中率 75.7% → 62.4%(请求交错导致共享 30-shot 前缀更常被驱逐) |

**为什么列为 P0**:这 13.3% 全靠一个命令行参数。换个人跑、或忘了加,就直接退回 554s。

原理见 [README.md](README.md) 与实验总结 §8.4:任务层(`run.py:142`)与 API 槽位层(`model.py:280` 的共享信号量)含义不同,默认却被绑成同一个数 4,导致 planner 串行门阻塞时腾出的 API 槽位**没有任务能接手**。任务层必须**严格大于** API 层,槽位才有队列可取。

### P0-2 提示词禁止把原文片段抄进子问题

| 项 | 内容 |
|---|---|
| 证据 | tw8 共 28 次 `planner_rejected`,其中 **9 次**是"子问题新增了原题和依赖答案未要求的年份限制" |
| 根因 | planner 把原文连同日期抄进子问题:`...The passage lists 'Moscow, February 28, 1908San Diego, July 26, 2005'...` → 校验器提取出 1908/2005,发现既不在原题也不在上游答案中 |
| 动作 | 在 `data_prompts/decompose.txt` 明确"子问题不得内嵌原文片段或其中的日期" |
| 附带 | 给 `planner_rejected` 事件**加上被拒动作本身**(目前只存 `error`),否则无法定量分析 |

**⚠️ 这不是校验器 bug。** 提示词(`data_prompts/decompose.txt:21`)与校验器(`graph.py:45`)规则一致,都要求新年份只能来自原题或已成功依赖节点的答案。属**模型过度引用原文**的行为问题。
(该结论基于 1 道题 3 条落盘实例——其余运行的 `rejected_actions` 在成功时被清空而丢失,样本很小。)

### P1-1 `invalid_actions` 改为累计计数

| 项 | 内容 |
|---|---|
| 现状 | `decompose.py:519` 在**任何**非 wait 动作成功后把 `invalid_actions` 归零 |
| 后果 | "连续 3 个不合法"只在**真正连续**时才触发;planner 交替产出合法/非法动作时计数被不断重置 |
| 实例 | `3hop1__145212_443779_52195` 在 tw8 下烧掉 **11 次 planner / 56.0s 服务 / 143.9s 墙钟**(6 次被拒)才失败 |
| 动作 | 改为累计计数,或同一节点被拒 ≥2 次即判失败 |

tw8 残留的 **59 空闲槽秒全部集中在尾部**,主要来源就是这类长尾任务。这是尾部问题,不是调度问题——所以下一步不该继续加并发。

### P1-2 减少 planner 回合数(精度换时间,慎用)

planner 占大模型服务 **62.3%**(1161.1s / 232 次),作答占 37.7%(701.7s / 157 次)。这是唯一的大杠杆。

| 动作 | 预期 | 风险 |
|---|---|---|
| 更激进 `add_batch`(一次 2~3 跳) | planner 回合数约减半 | **削弱增量反馈**。planner 的"重"是 DeRoute 比 DecomP 准的根本原因,不是无谓浪费 |

### P1-3 `small_route_mode=parallel_only` 实测

开关已存在(`decompose.py:552`),从未实测。思路:只有并行分支才允许派小模型。需要验证的是**墙钟收益 vs 大模型调用增幅**,不能只看调用数。

### P2-1 API 槽位提到 6~8

需加钱。**一旦改动,与 DecomP 的公平性必须重新论证**(DecomP 用 4 进程)。

### P2-2 小模型量化 / vLLM 连续批处理

进一步降小模型单次延迟。小模型当前 194 次调用 / 222.7s,不占大模型槽位。

---

## 已证伪 / 已否决

**不要再做这些**,列在这里是为了避免重复投入。

| 提议 | 结论 | 证据 |
|---|---|---|
| 裁 planner 的 30-shot 前缀 | **已证伪** | 裁完墙钟只 −0.7%。planner 延迟 **71% 是 4.31s 固定开销**(网络 RTT + 端点排队 + TTFT),与 prompt 长度无关 |
| 缩短 `max_tokens` | **无效** | 同上。planner 不是 prefill 绑定,是固定开销绑定 |
| 裁 `nodes[].output` 字段 | **只省成本,不省时间** | miss token −19.1%、大模型服务 −4.4%,但墙钟 −0.7%。已实施(`8d6f776`) |
| 砍 verifier | **部分已做,不再扩大** | `8d6f776` 砍的是大模型 **self-verify**(同一模型同一上下文,冗余且误杀正确答案,如 "1932")。全部复核**不可**砍——小模型 `inferred` 的 fallback 复核是唯一保留的路径 |
| 小模型上 13B | **不上** | — |
| 9/17 消融扫描 6 组(no-verify / quotefix / afrelax / stabletopk / largetopk10 / 15shot) | **均未采纳** | 没有一组在墙钟上优于对照;单点 EM 差异在 ±8 pt 噪声内 |

---

## 结构性事实(决策前提)

- **planner 是硬串行门**:229 次 planner 调用中 `continue_planning` 触发 **0** 次。
- **"四路并发"里平均只有约 1.5 路在真正答题**:tw8 的 1922 墙钟槽秒 = planner 1161s(62.3%)+ 作答 702s(37.7%)+ 空闲 59s。
- **29% 的 planner 调用是恢复驱动**(232 次中 68 次发生在该题首次拒绝/修订之后),不是向前规划。
- **已撞到填充下界**:1862.8s ÷ 4 槽 = **466s**,几乎正好等于 DecomP 同日的 ~460s。在 API 4 并发、planner 与作答共用同一槽池的前提下,**墙钟没有余量了**。
- **端点速度会漂移**:同样配置的 tw4,9/17 跑 648.0s、9/21 跑 554.0s,**纯端点差 14.5%**。跨日期比墙钟无效。

---

## 已完成

| 提交 | 内容 | 效果 |
|---|---|---|
| `8d6f776` | 裁 `nodes[].output` 为 `{answer, support_type}`;移除大模型 self-verify | miss token −19.1%;每节点省 1 次调用 |
| `8d6f776` | 修订上限降到 `max_revisions=1, max_total_revisions=2` | 比 2/3 档快 20%、只掉 4 EM(甜点) |
| `50081a1` | `large.max_concurrent` 2 → 4,与 `max_inflight_tasks` 对齐 | 消除 API 槽排队 |
| `bbc4e16` | 减少 planner 调用 | — |
| `cd63607` | 修复 `agent.sh` 指向不存在的 conda 环境(`agent` → `rag310`,可用 `DEROUTE_CONDA_ENV` 覆盖) | `./agent.sh` 可直接运行 |
| `61f364d` | 删除孤儿提示词 `prompts/decompose.txt` 与未使用的导入 | — |
| — | **任务层 / API 槽位层解耦**(`--task-workers 8`) | **墙钟 −13.3%**;填充率 3.44 → 3.88;爬坡段与中段空闲归零 |

> `outputs/` 被 `.gitignore` 忽略,实验目录不进版本控制。已清理 24 个被取代的实验目录。
